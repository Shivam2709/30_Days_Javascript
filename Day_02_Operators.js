
// Activities 1:-
// Task1: Write a progaram to add two number and log the result to the console.

    console.log(5 + 7);

// Task2: Write a progaram to Subtract two number and log the result to the console.

    console.log(7 - 3);

// Task3: Write a progaram to Multiply two number and log the result to the console.

    console.log(5 * 3);

// Task4: Write a progaram to Divide two number and log the result to the console.

    console.log(20 / 4 );

// Task5: Write a progaram to find the remainder when one number is divided by another and log the result to the console.

    console.log( 23 % 4);

// Task6: Use the += operator to add a number to variable and log the result to the console.

    let number = 5;
    number += 3; // Adds 3 to number
    console.log(number); // Output: 8

// Task 7: Use the -= operator to subtract a number from a variable and log the result to the console.

    let number2 = 10;
    number2 -= 4; // Subtracts 4 from number
    console.log(number2); // Output: 6

// Task 8: Write a program to compare two numbers using > and < and log the result to the console.

    let a = 5;
    let b = 10;
    console.log(a > b); // Output: false
    console.log(a < b); // Output: true

// Task 9: Write a program to compare two numbers using >= and <= and log the result to the console.

    let c = 10;
    let d = 10;
    console.log(c >= d); // Output: true
    console.log(c <= d); // Output: true

// Task 10: Write a program to compare two numbers using == and === and log the result to the console.

    let x = 10;
    let y = "10";
    console.log(x == y); // Output: true (only compares value)
    console.log(x === y); 

// Task 11: Write a program that uses the && operator to combine two conditions and log the result to the console.

    let a1 = 5;
    let b1 = 10;
    console.log(a1 < 10 && b1 > 5);

// Task 12: Write a program that uses the || operator to combine two conditions and log the result to the console.

    let a2 = 5;
    let b2 = 10;
    console.log(a2 > 10 || b2 > 5);

// Task 13: Write a program that uses the ! operator to negate a condition and log the result to the console.

    let num = 5;
    console.log(!(num > 10)); 

// Task 14: Write a program that uses the ternary operator to check if a number is positive or negative and log the result to the console.

    let number3 = -5;
    let result = (number3 >= 0) ? "Positive" : "Negative";
    console.log(result); 